// Cloudflare Worker entrypoint for TanStack Start.
// Wrangler expects wrangler.jsonc -> main = "src/server.ts" to exist.
// TanStack Start/Vite builds and mounts the SSR server handler from its internal entry.
//
// This file is intentionally minimal: the framework-generated server bundle is used at runtime.

export default {};
